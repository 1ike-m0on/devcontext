# Docs Only Service

This fixture intentionally contains documentation without source code, SQL,
configuration classes, repository implementations, or tests.

It exists so context benchmark boundary cases can verify that DevContext reports
missing primary evidence instead of selecting README, docs, or generated AI
summaries as source evidence.
